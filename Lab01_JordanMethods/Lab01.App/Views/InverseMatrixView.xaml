<UserControl x:Class="Lab01.App.Views.InverseMatrixView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Section header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <StackPanel>
                <TextBlock Text="Inverse Matrix" Style="{StaticResource TitleText}" Margin="0,0,0,4"/>
                <TextBlock Text="A⁻¹" FontSize="32" Foreground="#00D4AA" FontWeight="Light" Opacity="0.95"/>
            </StackPanel>
        </Grid>

        <!-- Cards row: Input (main) + Side panel (actions + result) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="16"/>
                <ColumnDefinition Width="*" MinWidth="300"/>
            </Grid.ColumnDefinitions>

            <!-- Main input card -->
            <Border Grid.Column="0" Style="{StaticResource FeatureCard}" Margin="0,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0">
                        <TextBlock Text="Input Matrix" Style="{StaticResource SectionHeader}"/>
                        <TextBlock Text="rows on new lines, numbers separated by spaces" 
                                   FontSize="13" Foreground="#FFF2F2F5" Margin="0,-8,0,8"/>
                    </StackPanel>
                    <TextBox Grid.Row="1" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" 
                             AcceptsReturn="True" Style="{StaticResource InputTextBox}"
                             VerticalScrollBarVisibility="Auto" VerticalAlignment="Stretch"
                             MinHeight="140" FontSize="14"/>
                </Grid>
            </Border>

            <!-- Right panel: actions + result cards -->
                    <StackPanel Grid.Column="2" VerticalAlignment="Stretch">
                <Border Style="{StaticResource SmallCard}" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Actions" Style="{StaticResource SectionHeader}"/>
                        <Button Command="{Binding ComputeCommand}" Style="{StaticResource ActionButton}" 
                                HorizontalAlignment="Stretch" Margin="0,0,0,8">
                            <TextBlock Text="Run Computation" FontSize="14" VerticalAlignment="Center"/>
                        </Button>
                        <Button Command="{Binding SaveProtocolCommand}" Style="{StaticResource SecondaryButton}" 
                                HorizontalAlignment="Stretch" Margin="0,0,0,8">
                            <TextBlock Text="Save Protocol" FontSize="14" VerticalAlignment="Center"/>
                        </Button>
                        <TextBlock Text="{Binding Status}" Foreground="#FFF2F2F5" FontSize="12" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource ResultCard}" VerticalAlignment="Stretch">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Result" Style="{StaticResource SectionHeader}"/>
                        <TextBox Grid.Row="1" Text="{Binding ResultText, Mode=OneWay}" IsReadOnly="True"
                                 Style="{StaticResource InputTextBox}" Foreground="#FFFFFF"
                                 MinHeight="100" VerticalScrollBarVisibility="Auto" 
                                 VerticalAlignment="Stretch" Margin="0,4,0,0"/>
                    </Grid>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
