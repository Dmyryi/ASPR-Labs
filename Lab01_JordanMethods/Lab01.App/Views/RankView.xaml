<UserControl x:Class="Lab01.App.Views.RankView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,24">
            <StackPanel>
                <TextBlock Text="Matrix Rank" Style="{StaticResource TitleText}" Margin="0,0,0,4"/>
                <TextBlock Text="r(A)" FontSize="32" Foreground="#00D4AA" FontWeight="Light" Opacity="0.95"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="280"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource FeatureCard}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel Grid.Row="0">
                        <TextBlock Text="Input Matrix" Style="{StaticResource SectionHeader}"/>
                        <TextBlock Text="rows on new lines, numbers separated by spaces" 
                                   FontSize="12" Foreground="#8A8A94" Margin="0,-8,0,8"/>
                    </StackPanel>
                    <TextBox Grid.Row="1" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}" 
                             AcceptsReturn="True" Style="{StaticResource InputTextBox}"
                             MinHeight="140" VerticalScrollBarVisibility="Auto" VerticalAlignment="Stretch"/>
                </Grid>
            </Border>

            <StackPanel Grid.Column="2" VerticalAlignment="Stretch">
                <Border Style="{StaticResource SmallCard}" Margin="0,0,0,12">
                    <StackPanel>
                        <TextBlock Text="Actions" Style="{StaticResource SectionHeader}"/>
                        <Button Command="{Binding ComputeCommand}" Style="{StaticResource ActionButton}" 
                                HorizontalAlignment="Stretch" Margin="0,0,0,8">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <TextBlock Text="&#xE768;" FontFamily="Segoe MDL2 Assets" FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0"/>
                                <TextBlock Text="Run Computation" FontSize="14" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                        <TextBlock Text="{Binding Status}" Foreground="#8A8A94" FontSize="12" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource SmallCard}" VerticalAlignment="Stretch">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Text="Result" Style="{StaticResource SectionHeader}"/>
                        <TextBlock Grid.Row="1" Text="{Binding ResultText}" Foreground="#00E676" FontSize="28" 
                                   FontWeight="SemiBold" FontFamily="Consolas" VerticalAlignment="Center" Margin="0,8,0,0"/>
                    </Grid>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
